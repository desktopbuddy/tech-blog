<section class="main-section">
  <h3>Dashboard</h3>
  <div class="create-post">
    <a href="/dashboard/new"><button class="btn primary-btn">+ Create new post</button></a>
  </div>
  <div>
  {{#each posts as |post|}}
  {{> post-details}}
  {{/each}}
  </div>
</section>